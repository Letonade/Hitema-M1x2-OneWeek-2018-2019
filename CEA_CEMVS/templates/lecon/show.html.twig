{% extends 'base.html.twig' %}

{% block title %}Lecon{% endblock %}

{% block body %}
    <h1>Lecon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lecon.id }}</td>
            </tr>
            <tr>
                <th>Present</th>
                <td>{{ EntrainementTireur.presence ? 1 : 0 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lecon_index') }}">back to list</a>


    {{ include('lecon/_delete_form.html.twig') }}
{% endblock %}