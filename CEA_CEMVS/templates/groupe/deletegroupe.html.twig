<form method="post" action="{{ path('groupe_delete', {'id': TireurGroupe.IdTrait}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ TireurGroupe.IdTrait) }}">
    <button class="btn">Delete</button>
</form>