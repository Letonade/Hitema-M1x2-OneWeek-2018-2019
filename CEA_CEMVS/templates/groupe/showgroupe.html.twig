{% extends 'base.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{groupe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('groupe_groupeIndex') }}">return to list OF groupe</a>

{% endblock %}